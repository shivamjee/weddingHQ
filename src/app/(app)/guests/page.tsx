import { EmptyState } from "@/components/EmptyState";

export default function GuestsPage() {
  return (
    <EmptyState emoji="🎉" title="Guests">
      Households, headcount, and tiers — turning &ldquo;who do we invite?&rdquo; into a running
      total and cost — will live here.
    </EmptyState>
  );
}
