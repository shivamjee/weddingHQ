import { EmptyState } from "@/components/EmptyState";

export default function BudgetPage() {
  return (
    <EmptyState emoji="💰" title="Budget">
      Per-side allocations, expenses, and who owes whom — with clear &ldquo;are we on track?&rdquo;
      numbers — will live here.
    </EmptyState>
  );
}
