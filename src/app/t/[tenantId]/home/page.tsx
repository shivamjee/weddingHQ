import { EmptyState } from "@/components/EmptyState";

export default function HomePage() {
  return (
    <EmptyState emoji="🏡" title="Home">
      Your wedding at a glance — a countdown, what needs your attention, and a budget snapshot will
      live here.
    </EmptyState>
  );
}
